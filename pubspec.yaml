name: parking_lot
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  intl: ^0.16.1
  js: ^0.6.2
  equatable: ^1.2.5
  sprintf: "^4.0.0"

dev_dependencies:
  mockito: ^3.0.0
  test: ^1.15.2
  build_runner: ^1.10.1